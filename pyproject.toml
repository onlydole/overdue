[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "overdue"
version = "0.1.0a1"
description = "Don't let your knowledge expire. A FastAPI-powered knowledge library game."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [
    { name = "Taylor Dolezal", email = "117500+onlydole@users.noreply.github.com" },
]
keywords = ["knowledge", "library", "fastapi", "game"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: FastAPI",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games/Entertainment",
]
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.1",
    "sqlalchemy[asyncio]>=2.0.51",
    "aiosqlite>=0.22.1",
    "pydantic[email]>=2.13.4",
    "pydantic-settings>=2.15.0",
    "PyJWT>=2.12.1",
    "bcrypt>=5.0.0,<5.1.0",
    "typer>=0.27.1",
    "rich>=15.0.0",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.28",
    "httpx>=0.28.1",
    "itsdangerous>=2.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.2",
    "mypy>=2.3.0,<3.0.0",
    "PyYAML>=6.0",
    "tree-sitter>=0.26.0",  # QueryCursor + dict-shaped captures landed here
    "tree-sitter-typescript>=0.23",
]

[project.scripts]
overdue = "src.cli.main:app"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
# SVG path data is a single unbreakable token -- splitting a `d="..."` string
# across lines to satisfy the line limit hurts readability and invites typos.
"src/game/icons/_catalog.py" = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
strict = true
